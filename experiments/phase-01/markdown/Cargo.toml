[package]
name = "spike-markdown"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

# 独立实验 crate：不属于生产 workspace（删除本目录不影响生产构建）。
[workspace]

[dependencies]
comrak = "0.54.0"
ratex-parser = "0.1.14"
ratex-layout = "0.1.14"
ratex-render = { version = "0.1.14", features = ["embed-fonts"] }
