# docs/reference/AGENTS.md

`docs/reference/` 存放外部技术参考资料（上游库文档要点、平台 API 笔记等）。

规则：

- 本目录内容**永远不得覆盖 `docs/plan/`**；与 plan 冲突时以 plan 为准。
- 引用外部资料必须注明来源与获取日期；外部内容可能过期。
- 不得把参考笔记写成契约；契约只能产生于 `docs/plan/` 并经审批。
