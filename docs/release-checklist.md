# StickyMD Release Checklist

## Automated gates

- [ ] Phase 12 source-controlled work is committed and an exact clean `RELEASE_SOURCE_COMMIT` receipt exists.
- [ ] `cargo fmt`, strict Clippy, workspace/Release tests, `cargo deny check`, and Rust `all --ci --json` pass on the exact Phase 12 candidate.
- [ ] Phase 12 Release/package、headless CI、performance、runtime、resources 五类 receipt 分别通过，
      且绑定 exact source commit 与适用的 EXE/ZIP SHA-256。
- [ ] Exact Phase 12 portable ZIP passes `tools/release/verify-package.ps1`.
- [ ] Exact Phase 12 `SHA256SUMS.txt` covers exactly the ZIP and `SBOM.spdx.json`.
- [ ] Exact Phase 12 SPDX SBOM was generated by the pinned, checksum-verified Syft version.
- [x] Release workflow syntax/static security audit passes; remote workflow evidence is recorded separately.

## Manual gates

- [ ] Every release-blocking `NOT TESTED` row in `docs/acceptance-cases/phase-12.md` has an exact-artifact receipt, or an explicitly enumerated USER waiver.
- [ ] Microsoft Pinyin and WeChat IME matrix.
- [ ] Light/Dark/System, opacity, Preview/math/image visual matrix.
- [ ] Tray, three docking edges, hover timing, DPI and real multi-monitor topology.
- [ ] Clipboard sources, export dialog, hard-kill recovery, junction/symlink and clean Windows 11 VM.

## USER decisions

- [ ] Approve the release version; do not infer `1.0.0` from the v1 product name.
- [ ] Exact-candidate cold startup p95 passes the USER-approved 400 ms gate. Phase 11 的 300.692 ms
      是历史证据；最新已作废候选的 812.599 ms 诊断结果为 FAILED。
- [ ] Exact-candidate warm startup p95 passes the USER-approved v0.1.0 400 ms hard boundary；180 ms
      remains preferred。Phase 11 的 311.353 ms 是历史证据；最新已作废候选的 531.779 ms
      诊断结果为 FAILED。400 ms 是 gate recalibration，不是 manual waiver。
- [ ] Approve every remaining manual waiver individually.
- [ ] Decide whether the unsigned-build policy is acceptable or provide a real code-signing certificate.
- [ ] Approve pushing the exact commit and tag.

## GitHub draft release

- [ ] Confirm the tag version exactly matches the workspace version and its commit belongs to the approved branch.
- [ ] Confirm the remote release workflow passed for the exact tag SHA.
- [ ] Download artifacts; verify `SHA256SUMS.txt` and run `gh attestation verify <artifact> --repo <owner/repo>`.
- [ ] Re-run the portable smoke on the downloaded ZIP and a clean Windows machine.
- [ ] Inspect the generated draft release and release notes.
- [ ] Publish the draft only after explicit USER approval.
